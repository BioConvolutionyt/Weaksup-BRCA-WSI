# 核心计算（torch 为 cu121 版本；CPU 环境装同名 torch 即可）
torch==2.2.2
numpy==1.26.4          # 必须 <2（wsinfer / zarr 兼容约束）
pandas==2.2.3
scikit-learn==1.6.1
scipy==1.15.2
scikit-image==0.25.2
matplotlib==3.10.3
pillow==11.1.0
opencv-python==4.9.0.80
zarr==2.18.7           # 必须 <3
numcodecs==0.15.1

# 病理图像与特征提取
openslide-python==1.4.1   # 另需系统级 OpenSlide 动态库，见 README
timm==1.0.19
huggingface-hub==0.36.2
wsinfer==0.6.1            # 面积口径参照（评估用）
wsinfer-zoo==0.6.3
# TRIDENT：按源码安装（git clone https://github.com/mahmoodlab/TRIDENT），
# 本项目基于 2026-08 的 main 分支运行，详见 README「环境安装」一节。

# 分析、QC 与报告
umap-learn==0.5.7
imagehash==4.3.2
python-docx==1.2.0
PyMaxflow==1.3.2          # 可选：探索 6.4 的拉格朗日图割近似
